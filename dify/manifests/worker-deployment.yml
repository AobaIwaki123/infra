apiVersion: apps/v1
kind: Deployment
metadata:
  name: dify-worker
  namespace: dify
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dify-worker
  template:
    metadata:
      labels:
        app: dify-worker
    spec:
      containers:
      - name: worker
        image: langgenius/dify-api:1.9.1
        env:
        - name: MODE
          value: "worker"
        envFrom:
        - configMapRef:
            name: dify-config
        - secretRef:
            name: dify-secret
        volumeMounts:
        - name: dify-storage
          mountPath: /app/api/storage
      volumes:
      - name: dify-storage
        persistentVolumeClaim:
          claimName: dify-storage
