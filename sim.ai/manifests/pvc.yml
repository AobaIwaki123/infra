apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sim-ai-postgres-pvc
  namespace: sim-ai
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-rbd
